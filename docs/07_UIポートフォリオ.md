# UIポートフォリオ（未承認）— 案A と 別案B

作成日：2026-09-25。前提資料：[05_現状の確認.md](05_現状の確認.md)、[06_認識合わせの質問.md](06_認識合わせの質問.md)（30問の回答）

- 案A のキャンバス：https://claude.ai/artifact/TY4A6KaZScvrHLgLteyoEw （非公開。オーナーだけが開ける）
- 別案B のキャンバス：https://claude.ai/artifact/17R9ZWAfg6GoPSRNK7vxZm （参考画像から組み直した案。[08_参考から読み取ったこと.md](08_参考から読み取ったこと.md)）
- 別案C のキャンバス：https://claude.ai/artifact/8TkmZoLtDymQvZkTieSYwG （全ジャンル前提で組み直した案。[09_別案C_全ジャンル前提.md](09_別案C_全ジャンル前提.md)）
- 元のファイル：`design/portfolio/src/`（`node --experimental-strip-types build.mjs` で `design/portfolio/project/` に書き出す。書き出したものと撮影はリポジトリに入れない）
- 手元の確認：`node shot.mjs`（`design/portfolio/preview/` を撮り、はみ出し・11px未満を数える）

## 案A のコンセプト

**買った人の声で描いた、商品の地図。** 順位ではなく位置で見せる。

1. 似ている・違うが見える — 近さ＝買った人の声（観点ごとの満足・不満の出方）の似かた。好きな商品を真ん中に置くと、違いが方角で分かる
2. 不満が先に見える — 商品の丸の周りの「声のリング」は、不満（オレンジ）から描く
3. 数字はごまかさない — 件数は必ず「読んだ数」と一緒。AI 分類を明示。引用は原文と照合

CLAUDE.md の4点（分母・散らばり・本文を出さない・アクセス制御）は変えていない。

## 中身

| ページ | 内容 |
|---|---|
| ポートフォリオ | 00 決めたこと〜21 要件の対応表（22枚） |
| 案の比較 | コンセプトの3案、地図の5案、見た目の3案（仮） |
| 画面（実寸） | スマホ14、PC 4 |
| 触れる試作 | 地図 → レンズ → 並べる（点が動く）→ 押す → 似ているもの |

地図・件数・引用は `data/` の実データで描いた（色違い3組は手でまとめた）。商品画像は載せず図形で代用。

## 測ったこと

| 項目 | 値 |
|---|---|
| 隣3つが同じブランドの割合（この地図） | 3%（偶然なら3%） |
| 同（前身の UMAP） | 29.9%（偶然なら4.2%） |
| 2次元に残せた情報 | 43% |
| 地図に載る商品 | 20（声が厚い6・ふつう3・点線11） |

**まだ言えないこと：** 「近い＝本当に似た商品か」は確かめきれていない。仕様の「開放型」で5件以上ふれられた商品が2つしかない。地図の公開ゲート（案）はボード07。イヤホンは今、未達。

## 未確認・未実施

- 案A は参考画像を見る前に作ったため、見た目（色・文字）とボード03は仮のまま。参考から組み直したのが別案B
- 埋め込みは未測定（レビュー本文がこの環境に無い）
- 引用の満足・不満の区別は今のデータに無い。画面の引用は手で分けた見本
- 名前の商標・ドメインは未確認
- ジャンルの候補（ボード09）は未測定の例
- 実装・公開はしていない。承認後に進める

## 別案B（参考から）

**モノの地図に、声を重ねる。** 参考（好き11・普通2・嫌い9）から、案Aを組み直した別案。案Aは残し、B12 のボードで「B で進める／A で進める／混ぜる」を選べるようにした（推すのは B）。

| | 案A | 案B |
|---|---|---|
| 地図の点 | 丸い点＋名前＋色のリング | 商品の画像。名前は押したときだけ |
| 近さ | 位置 | 位置＋点線（いちばん声が近い商品） |
| 観点の選び方 | チップの帯 | 下の輪を回す（片手） |
| 不満の見せ方 | 全商品にリング | 観点を選んだときだけ半円のゲージ（左から不満） |
| 2つの観点で並べる | 四象限 | 棚（1つの観点で4段）。四象限はPCの奥に |
| 分母 | 文字 | 声の束（1件＝1枚の板） |
| 読めていない商品 | 「まだ地図にない」 | 空きのタイル（97マスのうち26が絵） |
| 色 | 青とオレンジを全体に | ほぼ白黒。声の2色は印だけ |

- 元のファイル：`design/portfolio/src/b/`（`cd design/portfolio/src/b && node --experimental-strip-types buildb.mjs` で `design/portfolio/project-b/` に書き出す。確認は `node shotb.mjs`）
- 変えないもの：CLAUDE.md の4点、ゲート（軸・地図）、数え方、ジャンルの区分、お金と広告のルール（案A のボード 07〜09・17・18）
- 商品の絵は商品名の色から描いた代わりの絵。実装では楽天の商品画像
- **束を正確に描くには、レビュー1件ずつの分類を残す必要がある**（今のデータは観点ごとの合計だけ）。抽出のやり直しで一緒に残す
