# レビュー比較サイト

購入者レビューを観点ごとに数えて、商品を比較できるサイト。

**まず `docs/01_仕様.md` と `docs/02_測定済みの事実.md` を読むこと。**
02 には、実際に走らせて分かった落とし穴が全部書いてある。再発見しようとすると丸一日かかる。

## 絶対に外してはいけない4点

1. **数字は「N件中M件」。分母を常に出す。** 件数は集計であって推定ではないので「推定」バッジは付けない
2. **軸は言及の多さではなく意見の散らばりで選ぶ。** 最も語られる観点が最も軸に向かない（実測）
3. **レビュー本文は表示しない。** 件数と、原文と照合済みの短い引用だけ（著作権法30条の4）
4. **相手のアクセス制御は回避しない。** 403 やブロックページが返ったらそこで止める

設計判断は `.claude/skills/review-site-design/SKILL.md` に集約してある。
UI・観点設計・データ取得の作法に関わる作業では必ず読むこと。

## 作業の進め方

- **判断はまず測る。** この一式の数字はすべて実測。推測で設計を決めない
- **純粋関数に判断を集める。** `lib/aspect-model.ts`・`lib/nakami-model.ts` がその形。テストで固定する
- **できなかったことは、できなかったと書く。** 静かに範囲を狭めない

## ディレクトリ

```
docs/          仕様・実測値・決定事項・データ取得手順
scripts/       データ取得（楽天を読む・Gemini で数える）と、公開用の書き出し（docs/13）
reference/     前身プロジェクトのコードの残り（使うものは lib/ と scripts/ に移した）
data/          実データ（商品97件・観点24商品ぶん）
.claude/skills/review-site-design/  設計原則
```

## 技術的な前提

- Node.js 22 以上。`--experimental-strip-types` で `.ts` を直接実行している
- 表示は数十点なので **Canvas を使わない**。HTML の絶対配置＋実際の `<button>`
- 文字サイズの下限は 11px
