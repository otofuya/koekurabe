# Claude Code への最初の指示文

下の枠の中をそのままコピーして貼る。

---

```
このフォルダにある資料をもとに、購入者レビューを解析して商品を比較するサイトを作ります。

まず読んでほしいもの（この順で）:
1. CLAUDE.md
2. docs/01_仕様.md         — 何を作るか
3. docs/02_測定済みの事実.md — 実際に走らせて分かったこと。ここがいちばん重要
4. docs/03_決定事項.md      — 判断済みなので再検討しなくてよいこと
5. .claude/skills/review-site-design/SKILL.md — 設計原則

data/ に前身プロジェクトで実際に取得したデータが入っています。
クロールも Gemini 呼び出しもせずに画面を作り始められます:
- data/genre-products.json  完全ワイヤレスイヤホン97商品
- data/genre-aspects.json   24商品ぶんの観点集計（のべ1,780レビュー・引用つき）

reference/ には前身で動いていたコードがあります。特に aspect-model.ts は
収縮・分離度・軸ゲートの判断が入った純粋関数で、テストつきでそのまま使えます。

まず data/genre-aspects.json を実際に読んで中身を確認したうえで、
どう作るか計画を立ててください。私に質問があれば遠慮なく聞いてください。

最初の目標は /compare/earbuds（四象限チャート）を動く状態にすることです。
```

---

## 補足：貼ったあとの進め方

**最初の1手で全部を作らせない。** 順番はこれを勧める：

1. `lib/aspect-model.ts` を `reference/` から移し、テストを通す（判断の土台）
2. `data/genre-aspects.json` を読むランタイムと API
3. `/compare/:genre` の四象限（HTML絶対配置・Canvasは使わない）
4. `/reviews/:productId`（評判が上、仕様が下）
5. `/aspect/:genre/:aspect` 観点別ランキング
6. ホーム、`/vs/:a/:b`、OG画像

**2〜3のあいだで一度ブラウザで見ること。** 数字が画面に出た瞬間に、
分母の見せ方やカバレッジの扱いについて判断したくなる点が出る。

## 詰まったときに効く質問

- 「この観点は分離度いくつ？　軸に使える？」
- 「この商品の分母は何件？」
- 「引用は原文と照合済み？　何件捨てた？」
- 「カバレッジは画面に出てる？」

## 新しいジャンルを追加するとき

1. カテゴリ定義に観点を10個前後、**人手で**書く（極の言葉も一緒に）
2. `fetch-reviews.mjs` → `fetch-shop-reviews.mjs` → `extract-aspects.mjs`
3. 分離度を見て、軸に使える観点が2つ以上あるか確認
4. 無ければそのジャンルは公開しない
